<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            background-color: #cce0ff; /* Light blue background for full screen */
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .login-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 500px;
            width: 100%;
        }
        
        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }
        
        .logo {
            max-width: 300px;
            height: auto;
        }
        
        .login-form {
            width: 100%;
            background-color: #34495e; /* Dark blue form */
            padding: 40px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            color: white;
            box-sizing: border-box;
        }
        
        .welcome-text {
            text-align: center;
            margin-bottom: 25px;
            font-size: 24px;
            font-weight: bold;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        
        .form-group input {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 3px;
            background-color: #f0f5ff;
            box-sizing: border-box;
        }
        
        .login-button-container {
            text-align: center;
            margin-top: 25px;
        }
        
        .login-button {
            width: 100px;
            padding: 8px 15px;
            background-color: #cce0ff;
            color: #333;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }
        
        .login-button:hover {
            background-color: #b3d1ff;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="logo-container">
            <img src="/public/images/logo_kmdg.jpg" alt="logo_kmdg" class="logo">
        </div>
        
        <div class="login-form">
            <div class="welcome-text">SELAMAT DATANG</div>
            
            <form action="/login" method="post">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                
                <div class="login-button-container">
                    <button type="submit" class="login-button">Login</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>